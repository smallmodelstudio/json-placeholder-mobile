# Shared defaults, committed. Override per machine in .env.local (gitignored).
# localhost reaches the API from an emulator only via `adb reverse tcp:3000 tcp:3000`.
EXPO_PUBLIC_API_URL=http://localhost:3000
